# Cloud Functions runtime config (set via `firebase functions:config:set` or
# Secret Manager-backed params). Nothing is required for local emulator use;
# Vertex AI calls (classifyPhotos, M5) rely on the function's own service
# account / Application Default Credentials, not an API key here.
